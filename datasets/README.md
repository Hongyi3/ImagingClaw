# Datasets

The canonical dataset registry lives in `datasets/registry.yaml`.

Each registry entry must record:

- modality and measurement domain
- data kind
- access mode
- source and redistribution policy
- known preprocessing assumptions
- a dataset-specific card path

This directory is for metadata, cards, and access instructions. It is not for checked-in large
datasets.
